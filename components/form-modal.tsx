"use client";import{useState}from"react";import{X}from"lucide-react";
export function FormModal({button,title,children,autoOpen=false}:{button:string;title:string;children:React.ReactNode;autoOpen?:boolean}){const[open,setOpen]=useState(autoOpen);return <><button onClick={()=>setOpen(true)} className="min-h-11 rounded-full bg-acid px-5 py-3 font-bold text-ink">{button}</button>{open&&<div className="fixed inset-0 z-[80] grid place-items-end overflow-hidden bg-black/70 sm:place-items-center sm:p-5" onMouseDown={()=>setOpen(false)}><section onMouseDown={e=>e.stopPropagation()} className="max-h-[calc(100dvh-1rem)] w-full min-w-0 max-w-3xl overflow-y-auto overscroll-contain rounded-t-3xl bg-raised p-4 pb-[max(1rem,env(safe-area-inset-bottom))] sm:max-h-[92dvh] sm:rounded-3xl sm:p-5"><div className="mb-5 flex items-start justify-between gap-4"><h2 className="min-w-0 text-xl font-semibold sm:text-2xl">{title}</h2><button aria-label="Fechar" className="grid size-11 shrink-0 place-items-center rounded-full hover:bg-white/[.05]" onClick={()=>setOpen(false)}><X/></button></div>{children}</section></div>}</>}
