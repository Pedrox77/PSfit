import { ArrowLeft, ArrowRight } from "lucide-react";
import { PsfitLoader } from "@/components/ui/psfit-loader";
export function OnboardingNavigation({back,next,last=false,disabled=false,loadingLabel="Saving…"}:{back?:()=>void;next?:()=>void;last?:boolean;disabled?:boolean;loadingLabel?:string}){return <div className="sticky bottom-3 mt-8 flex gap-3 rounded-2xl border border-white/[.08] bg-[#090c0a]/95 p-3 backdrop-blur"><button type="button" onClick={back} disabled={!back||disabled} className="flex items-center gap-2 rounded-full border border-white/10 px-5 py-3 text-sm disabled:invisible"><ArrowLeft size={16}/>Back</button>{last?<button type="submit" disabled={disabled} aria-label={disabled?loadingLabel:undefined} className="ml-auto min-w-40 rounded-full bg-acid px-6 py-3 text-sm font-bold text-ink disabled:opacity-50">{disabled?<PsfitLoader size="sm" label=""/>:"Save and continue"}</button>:<button type="button" onClick={next} disabled={disabled} className="ml-auto flex items-center gap-2 rounded-full bg-acid px-6 py-3 text-sm font-bold text-ink disabled:opacity-50">Continue<ArrowRight size={16}/></button>}</div>}
