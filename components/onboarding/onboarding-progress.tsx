export function OnboardingProgress({current,total}:{current:number;total:number}){return <div><div className="mb-2 flex justify-between text-xs text-muted"><span>Step {current} of {total}</span><span>{Math.round(current/total*100)}%</span></div><div className="h-1.5 rounded-full bg-white/[.08]"><div className="h-full rounded-full bg-acid transition-all" style={{width:`${current/total*100}%`}}/></div></div>}
