import type { WorkoutReceipt as Receipt } from "@/types/database";
import { BadgeCheck, Dumbbell, Trophy } from "lucide-react";
export function WorkoutReceipt({ receipt, verified }: { receipt: Receipt; verified: boolean }) {
 return <section className="mx-4 mb-4 overflow-hidden rounded-2xl border border-acid/20 bg-[linear-gradient(145deg,rgba(168,255,42,.09),rgba(53,217,245,.04))] p-5"><div className="flex items-center justify-between"><p className="text-xs font-bold uppercase tracking-[.16em] text-acid">Workout completed</p>{verified&&<span className="flex items-center gap-1 text-[10px] text-aqua"><BadgeCheck size={13}/>Verified PSFIT Session</span>}</div><h3 className="mt-3 text-xl font-semibold">{receipt.title}</h3><div className="mt-4 grid grid-cols-3 gap-2 text-sm"><span>{receipt.duration_minutes} min</span><span>{receipt.exercises_completed} exercises</span><span>{receipt.total_volume_kg.toLocaleString()} kg</span></div><p className="mt-4 text-xs text-muted">{receipt.muscles.join(" · ")}</p>{receipt.personal_records>0&&<p className="mt-3 flex items-center gap-2 text-xs text-acid"><Trophy size={14}/>{receipt.personal_records} personal record{receipt.personal_records===1?"":"s"}</p>}<div className="mt-4 flex items-center justify-between border-t border-white/[.08] pt-4 text-xs text-muted"><span className="flex items-center gap-1"><Dumbbell size={14}/>Effort {receipt.effort}/10</span><span>Momentum {receipt.momentum_score}</span></div></section>;
}
