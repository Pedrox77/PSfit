import type { StoryHighlight } from "@/types/database";
import Image from "next/image";
export function HighlightsRow({items,own}:{items:StoryHighlight[];own:boolean}){if(!items.length&&!own)return null;return <section className="flex gap-4 overflow-x-auto border-b border-white/[.07] px-5 py-5 [scrollbar-width:none]">{items.map(item=><button key={item.id} className="w-16 shrink-0 text-center">{item.cover_url?<Image unoptimized src={item.cover_url} width={58} height={58} className="mx-auto h-[58px] w-[58px] rounded-full object-cover" alt={item.name}/>:<span className="mx-auto grid h-[58px] w-[58px] place-items-center rounded-full border border-white/15 bg-white/[.04]">{item.name[0]}</span>}<span className="mt-1 block truncate text-[11px]">{item.name}</span></button>)}{own&&<a href="/community/highlights/edit" className="w-16 shrink-0 text-center"><span className="mx-auto grid h-[58px] w-[58px] place-items-center rounded-full border border-dashed border-white/20">+</span><span className="mt-1 block text-[11px] text-muted">New</span></a>}</section>}
