"use client";
import { Check,LockKeyhole } from "lucide-react";
import Link from "next/link";
import { useTranslations } from "next-intl";
export type ProFeature="nutrition"|"twin"|"recovery"|"momentum"|"progression"|"analytics";
type Props={feature:ProFeature;title?:string;description?:string;benefits?:string[];backHref?:string};
export function ProFeatureGate({feature,title,description,benefits,backHref="/dashboard"}:Props){const t=useTranslations("ProFeatureGate");const items=benefits??(["benefit1","benefit2","benefit3","benefit4","benefit5"] as const).map(key=>t(`features.${feature}.${key}`));return <section className="mx-auto max-w-2xl rounded-3xl border border-acid/25 bg-[radial-gradient(circle_at_top,rgba(168,255,42,.12),transparent_55%),#090c0a] p-7 text-center sm:p-10"><span className="mx-auto grid size-12 place-items-center rounded-2xl border border-aqua/20 bg-aqua/[.06] text-aqua"><LockKeyhole/></span><span className="mt-5 inline-block rounded-full border border-acid/30 px-3 py-1 text-xs font-bold text-acid">{t("badge")}</span><h1 className="mt-4 text-3xl font-semibold">{title??t(`features.${feature}.title`)}</h1><p className="mx-auto mt-3 max-w-lg text-muted">{description??t(`features.${feature}.description`)}</p><ul className="mx-auto mt-6 grid max-w-lg gap-2 text-left sm:grid-cols-2">{items.map(item=><li key={item} className="flex items-center gap-2 rounded-xl border border-white/[.08] p-3 text-sm"><Check size={15} className="shrink-0 text-acid"/>{item}</li>)}</ul><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/settings/billing" className="inline-flex min-h-11 items-center justify-center rounded-full bg-acid px-6 font-bold text-ink">{t("viewPlans")}</Link><Link href={backHref} className="inline-flex min-h-11 items-center justify-center rounded-full border border-white/15 px-6">{t("back")}</Link></div></section>}
