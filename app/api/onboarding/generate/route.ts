import { generateInitialPlan,PlanGenerationError } from "@/lib/onboarding/generate-plan";
import { NextResponse } from "next/server";
export async function POST(){try{return NextResponse.json(await generateInitialPlan())}catch(error){if(process.env.NODE_ENV==="development")console.error("Plan generation error:",error);const step=error instanceof PlanGenerationError?error.step:"finalizing";const message=error instanceof Error?error.message:"Unknown generation error";const proRequired=message==="PRO_REQUIRED";return NextResponse.json({ok:false,step,errorCode:proRequired?"PRO_REQUIRED":undefined,error:proRequired?"Nutrition is available with PSFIT Pro.":process.env.NODE_ENV==="development"?message:"We couldn't generate your plan."},{status:step==="authentication"?401:proRequired?403:500})}}
