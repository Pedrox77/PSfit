import { z } from "zod";
export const workoutExerciseSchema=z.object({catalog_exercise_id:z.string().uuid(),name:z.string().min(1),sets:z.number().int().min(1).max(10),repetitions:z.string().min(1).max(30),rest_seconds:z.number().int().min(0).max(900),suggested_weight_kg:z.number().min(0).nullable(),load_guidance:z.string().max(300),notes:z.string().max(500)});
export const manualWorkoutSchema=z.object({name:z.string().min(2).max(100),description:z.string().max(1000),focus:z.string().max(200),difficulty:z.string().max(30),scheduled_weekday:z.number().int().min(0).max(6),scheduled_time:z.string().nullable(),estimated_minutes:z.number().int().min(5).max(300),exercises:z.array(workoutExerciseSchema).min(1)});
export const importedWorkoutSchema=z.object({title:z.string().nullable(),exercises:z.array(z.object({name:z.string().min(1),sets:z.number().int().nullable(),repetitions:z.string().nullable(),rest_seconds:z.number().int().nullable(),load_guidance:z.string().nullable(),notes:z.string().nullable()})).min(1)});
